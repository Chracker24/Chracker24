name: Update Daily Hello GIF

on:
  schedule:
    - cron: "0 0 * * *"  # Every day at midnight UTC
  workflow_dispatch:      # Allows manual run via GitHub UI

jobs:
  update-gif:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository using custom push token
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIF_PUSH_TOKEN }}  # Custom personal access token with repo write access

      - name: Set current day of the week
        id: date
        run: echo "day=$(date +%A | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Copy today's GIF into today.gif
        run: |
          cp hello-gifs/${{ steps.date.outputs.day }}.gif hello-gifs/today.gif

      - name: Commit and push changes
        run: |
          git config --global user.name 'Christy Chovalloor'
          git config --global user.email 'chracker24@users.noreply.github.com'
          git add hello-gifs/today.gif
          git commit -m "🔁 Daily Hello GIF updated: ${{ steps.date.outputs.day }}" || echo "No changes to commit"
          git push origin main
